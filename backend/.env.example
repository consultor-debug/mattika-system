# Copia a .env (local) o configura estas variables en EasyPanel (servicio mattika-app).
DATABASE_URL=postgresql://postgres:TU_PASSWORD@mattika_mattika-postgres:5432/mattika
# Genera uno con:  openssl rand -base64 32
JWT_SECRET=cambia-esto-por-un-secreto-largo-y-aleatorio-minimo-32-chars
PORT=3001
CORS_ORIGIN=*
